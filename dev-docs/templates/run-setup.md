# Dogfood #<n> — setup

How this run was set up, written before it starts. `runs/dogfood-protocol.md` is the protocol it
follows. Delete this paragraph when copying.

## 1. What is installed

The wheel, where it came from, and the copy this run holds under `<project>/wheels/` so a rebuild
here does not disturb it. The backend, the model, and the environment.

## 2. The task

What the coding agent is asked to build, and why this task rather than another.

## 3. The prompt

Verbatim, so the run can be repeated. Anything specific to Simple Agents belongs here rather than
in the build-log format, which has to serve the unguided arm too.

## 4. What this run is testing

The question the run exists to answer, written before it can be influenced by the result.
