# <subject>

`plan.md` §1 <P3-n>'s record. **Nothing is built.**

The record of an item while it is open. When the item is built, this file goes to `build-logs/` if
the build log now says everything it said, to `design/` if it is standing design of record, or to
`archive/` if it is superseded. It never stays here. Delete this paragraph when copying.

## Where it came from

The origin, quoted verbatim if it was written by someone. A finding record, a build log's Left
open, an analysis, or Thilina directly.

## What the problem is

What is wrong or missing today, with the evidence measured rather than asserted. Name the file and
the symbol.

## What has to be decided

The questions a design has to answer, listed so they are not discovered late.

## What it waits on

The other item, the measurement, or the decision that has to land first. `none` if nothing.
